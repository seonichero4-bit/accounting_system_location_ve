============================= test session starts ==============================
platform linux -- Python 3.12.1, pytest-9.1.1, pluggy-1.6.0 -- /workspaces/accounting_system_location_ve/venv/bin/python
cachedir: .pytest_cache
django: version: 6.0.7, settings: accounting_system_ve.settings (from ini)
rootdir: /workspaces/accounting_system_location_ve
configfile: pytest.ini
plugins: django-4.14.0, Faker-40.15.0
collecting ... collected 9 items

business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_001_generate_excel_stream_with_invoice ERROR [ 11%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_002_generate_excel_stream_with_debit_note ERROR [ 22%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_003_generate_excel_stream_with_credit_note ERROR [ 33%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_004_sequential_export_of_multiple_records ERROR [ 44%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_001_buffer_pointer_reset_to_zero ERROR [ 55%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_002_decimal_precision_and_zero_handling ERROR [ 66%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_003_strict_absence_of_visual_styles ERROR [ 77%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_004_preservation_of_leading_zeros_and_formats ERROR [ 88%]
business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_005_instantiation_with_null_dependencies_raises_error PASSED [100%]

==================================== ERRORS ====================================
_____ ERROR at setup of test_id_hp_001_generate_excel_stream_with_invoice ______

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 625>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
---------------------------- Captured stderr setup -----------------------------
Using existing test database for alias 'default'...
____ ERROR at setup of test_id_hp_002_generate_excel_stream_with_debit_note ____

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 626>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
___ ERROR at setup of test_id_hp_003_generate_excel_stream_with_credit_note ____

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 627>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
____ ERROR at setup of test_id_hp_004_sequential_export_of_multiple_records ____

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 628>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
________ ERROR at setup of test_id_ec_001_buffer_pointer_reset_to_zero _________

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 629>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
_____ ERROR at setup of test_id_ec_002_decimal_precision_and_zero_handling _____

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 630>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
_______ ERROR at setup of test_id_ec_003_strict_absence_of_visual_styles _______

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 631>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
__ ERROR at setup of test_id_ec_004_preservation_of_leading_zeros_and_formats __

db = None, fiscal_profile = <FiscalProfile: Empresa C.A. (J123456780)>
local_supplier = <LocalSupplier: Proveedor de Prueba (J123456780) - 632>

    @pytest.fixture
    def purchase_ledger_invoice(
        db: Any, fiscal_profile: FiscalProfile, local_supplier: LocalSupplier
    ) -> PurchaseLedgerInvoice:
        """Crea una factura de compra alineada a un periodo fiscal quincenal."""
        invoice = PurchaseLedgerInvoice(
            fiscal_profile=fiscal_profile,
            transaction_type=PurchaseLedgerInvoice.TransactionType.REGISTRO,
            document_type=PurchaseLedgerInvoice.DocumentType.INVOICE,
            number="00004567",
            invoice_control="00-00123",
            supplier=local_supplier,
            date=date(2026, 8, 10),
            fiscal_period=date(2026, 8, 15),
            purchase_type=PurchaseLedgerInvoice.PurchaseType.INTERNAL,
            taxable_base=Decimal("1000.00"),
            vat_percentage=PurchaseLedgerInvoice.VatPercentageChoices.GENERAL.value,
            vat_amount=Decimal("160.00"),
            total_purchase=Decimal("1160.00"),
            status=PurchaseLedgerInvoice.InvoiceStatus.PRELIMINARY,
            invoicecategory=PurchaseLedgerInvoice.InvoiceCategory.INVENTARIO,
        )
>       invoice.full_clean()

business_logic/tests/test_vat_withholding_excel_export_service/conftest.py:86: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <PurchaseLedgerInvoice: Factura N° 00004567 (00-00123) - ID: None>
exclude = {'affected_account'}, validate_unique = True
validate_constraints = True

    def full_clean(self, exclude=None, validate_unique=True, validate_constraints=True):
        """
        Call clean_fields(), clean(), validate_unique(), and
        validate_constraints() on the model. Raise a ValidationError for any
        errors that occur.
        """
        errors = {}
        if exclude is None:
            exclude = set()
        else:
            exclude = set(exclude)
    
        try:
            self.clean_fields(exclude=exclude)
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Form.clean() is run even if other validation fails, so do the
        # same with Model.clean() for consistency.
        try:
            self.clean()
        except ValidationError as e:
            errors = e.update_error_dict(errors)
    
        # Run unique checks, but only for fields that passed validation.
        if validate_unique:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_unique(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        # Run constraints checks, but only for fields that passed validation.
        if validate_constraints:
            for name in errors:
                if name != NON_FIELD_ERRORS and name not in exclude:
                    exclude.add(name)
            try:
                self.validate_constraints(exclude=exclude)
            except ValidationError as e:
                errors = e.update_error_dict(errors)
    
        if errors:
>           raise ValidationError(errors)
E           django.core.exceptions.ValidationError: {'affected_account': ['Este campo no puede estar vacío.']}

venv/lib/python3.12/site-packages/django/db/models/base.py:1712: ValidationError
=========================== short test summary info ============================
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_001_generate_excel_stream_with_invoice
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_002_generate_excel_stream_with_debit_note
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_003_generate_excel_stream_with_credit_note
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_hp_004_sequential_export_of_multiple_records
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_001_buffer_pointer_reset_to_zero
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_002_decimal_precision_and_zero_handling
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_003_strict_absence_of_visual_styles
ERROR business_logic/tests/test_vat_withholding_excel_export_service/test_vat_withholding_excel_export_service.py::test_id_ec_004_preservation_of_leading_zeros_and_formats
========================= 1 passed, 8 errors in 43.90s =========================
