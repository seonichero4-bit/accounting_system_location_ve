TESTING [

    1. Modelo "FiscalProfile": falta implementar test unitarios e integracion, a las validaciones y restriccion (clean() y save())
    2. Modelo "LocalSupplier": falta implementar test unitarios e integracion, a las validaciones y restriccion (clean() y save())
    3. Modelo "VatWithholdingCertificate": falta implementar test unitarios e integracion, a las validaciones y restriccion (clean() y save() y delete())
]

MODELOS[

    1. Modelo "PurchaseLedgerInvoice": falta implementar soporte para compra gravadas con multiples 
    alicuotas (detallar alicuotas).
    2. Modelo "PurchaseLedgerInvoice": falta implementar validaciones, sanetizacion y restricciones
    para el nuevo campo "affected_account", Tambien implenmentar logica de presentacion y actualizar
    test (unitario e integracion)
        VALIDACIONES:
            -No necesario en registro de nota de credito y debito.(se implemento el campo con blank=true
            por necesidades de dependencias, al implementar validaciones debe regresar a  blank=False)
    .
    3. Modelo "PurchaseLedgerInvoice": falta implementar validaciones, sanetizacion y restricciones
    para el nuevo campo "deductibility", Tambien implenmentar logica de presentacion y actualizar
    test (unitario e integracion).
    4. Modelo "PurchaseLedgerInvoice": falta implementar asignacion automatica de atributo "deductibility"
    a nota de credito y debito, desde la factura afectada.
    
]

SERVICIO[
    1.FiscalBatchProcessingService: falta implementar una validacion, para verificar que
    documentos "PurchaseLedgerInvoice" del tipo "SERVICIO" y "SERVICIO_MIXTO", de cualquier 
    tipo de contribuyente, posean un comprobante de rentencion de ISLR.
    2. PurchaseLedgerExcelBuilder: El codigo y logica del servicion debe ser refactoriazada.
]

MANAGER[
    1. RequestScopedManager: Falta implementar un plan de testing.
]

MIXINS[
    1.RequestScopedQuerySetMixin: Falta implementar un plan de testing.
]